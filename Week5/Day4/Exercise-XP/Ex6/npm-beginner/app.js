import chalk from 'chalk';

console.log(chalk.blue('Hello, World!'));
console.log(chalk.green('This is a simple Node.js application using chalk for colored output.'));
console.log(chalk.red('Make sure to install the chalk package using npm before running this script.'));